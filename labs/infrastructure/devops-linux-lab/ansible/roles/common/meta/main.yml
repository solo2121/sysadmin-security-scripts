---
galaxy_info:
  role_name: common
  author: solo2121
  description: >-
    Baseline OS configuration (packages, SSH service, timezone, /etc/hosts)
    shared by every node in the devops-linux-lab.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: EL
      versions:
        - all
    - name: opensuse
      versions:
        - all
dependencies: []
