diff a/docs/setup/installation.md b/docs/setup/installation.md	(rejected hunks)
@@ -309,13 +309,12 @@ vagrant status
 vagrant up
 ```
 
-**VirtualBox** (from the lab's `virtualbox/` subdirectory):
+**VirtualBox** (from the same lab directory, no subdirectory needed):
 
 ```bash
-cd virtualbox
-vagrant up dc01
+vagrant up dc01 --provider=virtualbox
 vagrant status
-vagrant up
+vagrant up --provider=virtualbox
 ```
 
 ### VLAN edition
@@ -383,14 +384,13 @@ cd security-engineering-lab/labs/infrastructure/devops-linux-lab
 vagrant up
 ```
 
-**VirtualBox** (from the lab's `virtualbox/` subdirectory):
+**VirtualBox** (from the same lab directory, no subdirectory needed):
 
 ```bash
-cd virtualbox
-vagrant up
+vagrant up --provider=virtualbox
 ```
 
-> Nested virtualization for the K3s/Kind/K3d workloads is enabled automatically in both Vagrantfiles, but on VirtualBox it depends on your host CPU exposing VT-x/AMD-V to the guest — see [Known limitations](../../README.md#known-limitations) in the main README.
+> Nested virtualization for the K3s/Kind/K3d workloads is enabled automatically for both providers in this Vagrantfile, but on VirtualBox it depends on your host CPU exposing VT-x/AMD-V to the guest — see [Known limitations](../../README.md#known-limitations) in the main README.
 
 ### Verify Lab 2
 
